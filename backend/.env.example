DATABASE_URL=postgres://tokenhub:tokenhub@localhost:5433/tokenhub
API_PORT=3001
APP_ORIGIN=http://localhost:3000
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
AI_PROVIDER=codex
CODEX_MODEL=
# Local development only
EMBEDDED_DB=false
